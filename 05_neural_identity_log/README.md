# 05 Neural Identity Log

```text
╔══════════════════════════════════════════════════════════════════╗
║                    NEURAL IDENTITY LOG NODE                     ║
║              OL-AB PUNK RECORD // EGGHEAD ACCESS                ║
╚══════════════════════════════════════════════════════════════════╝

> mounting neural identity log...
> reading memory-status records...
> calculating autonomy scores...
> checking personality retention...
> scanning command override layers...
> resolving identity-continuity conflicts...

NODE STATUS : ACTIVE
ACCESS MODE : LIMITED PUBLIC NODE
WARNING     : IDENTITY FIELDS MAY CONTAIN REDACTION DAMAGE
```

The **Neural Identity Log** records memory status, autonomy score, personality retention, command override, and identity continuity.

This is the archive node where the database stops asking only:

```text
What was engineered?
```

and starts asking:

```text
Who remains?
Who decides?
Who remembers?
Who was erased?
```

## Log Classes

```text
01 memory_status
02 autonomy_score
03 personality_retention
04 command_override
05 identity_continuity
```

## Core Rule

```text
a brain record is not a person
a memory score is not a life
a command response is not consent
a platform body may still hold an identity trace
```

## Fast Index

| Log folder | Main file | Purpose |
|---|---|---|
| `memory_status/` | `memory_status_index.csv` | Memory preservation, suppression, erasure, or non-inheritance |
| `autonomy_score/` | `autonomy_score_table.csv` | Autonomy index, command penalty, and review routing |
| `personality_retention/` | `personality_retention_log.csv` | Personality echo, continuity, fragmentation, and emergent identity |
| `command_override/` | `command_override_registry.csv` | Command layers, override scopes, authority seals, and blocked records |
| `identity_continuity/` | `identity_continuity_matrix.csv` | Continuity classification across original, clone, engineered, and converted subjects |

## Neural Warning

```text
If the archive stores the body but loses the memory,
it must mark the loss.

If the archive stores the command but loses the will,
it must mark the wound.
```
